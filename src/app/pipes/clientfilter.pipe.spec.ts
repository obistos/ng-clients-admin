import { ClientfilterPipe } from './clientfilter.pipe';

describe('ClientfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
